taskino database

taskino_company - id, name, plan_id_active, email, url_logo, codename, folder_name, 
                    payment_status[pending, confirmed, canceled], date_last_login, date_added
taskino_company_options - id, company_id, option_title, option_key, option_value, date_added
taskino_company_payments - id, company_id, description, plan_id, date_requested, staff_member_confirmed_id, date_confirmed

taskino_company_member - id, company_id, name, email, password, status[active, pending, inactive], is_admin, date_added, date_last_login
taskino_company_member_options - id, company_member_id, option_title, option_key, option_value, date_added

taskino_plans - id, name, description, price, max_tasks, max_filesize, status[active, inactive], date_added

taskino_staff_member - id, name, email, password, is_admin, status[active, inactive], date_last_login, date_added
taskino_staff_member_options - id, staff_member_id, option_title, option_key, option_value, date_added
taskino_support_faq - id, question, answer, support_topic_id, author_name, author_email, staff_member_id, date_approved, date_added
taskino_support_topic - id, name, staff_member_id, date_added

notifications - id, from_member_id[0=system], to_member_id, message, notify_type[notification, alert], date_added, date_removed

go_task_categories


//has_permission(user_id, 'read', 'this::task_add') // read, edit, remove
//add_permission(user_id, 'read', 'this::task_remove')
//remove_permission(user_id, 'read', 'this::task_remove')

project_permission - id, project_id, user_id(array_json), 
task_permission - id, task_id, user_id(array_json), 

task_has_permission(task_id, member_id)
task_add_permission(task_id, member_id)


Atividades
- Criar categorias para as tarefas;
- Criar projetos que vao possuir tarefas;
- Associar tarefas a um projeto, tornar obrigatorio;
- Admin define o que cada membro pode ver(acessar);
- Remover link para membros do menu, somente Admin pode ver;

- Criar cadastro mega basico - nome empresa, Admin name, email, senha;
- Associar membros a uma empresa;
- Criar sistema de planos - add X projetos, usar X Gb de espaço;
- Criar configurações da empresa e configurações do membro/usuario;
- Criar admin para in9web acessar e gerenciar clientes cadastrados;

Plan Basic
  Name: Basic
  Price: Free
  Total Tasks: 10
  File Storage: 100 MB

Plan Slim
  Name: Slim
  Price: R$ 9,90 /month
  Total Tasks: 50
  File Storage: 500 MB


NAO PRECISA MAIS
taskino.vs/app/elisa

RewriteRule ^/app/(.*)$ /taskino-content/$1/index.php/$2

RewriteCond %{HTTP_HOST} !^www\.yourwebsite.com
RewriteCond %{HTTP_HOST} ([^.]+)\.yourwebsite.com
RewriteRule ^/app/(.*)/(.*)$ /path_to_your_site/httpdocs/work_out.php?url=%1

